topics:
  reservation:
    request:  hotel-reservation-request
    response:  hotel-reservation-response

spring:
  datasource:
    url: jdbc:postgresql://localhost:8787/
    password: postgres
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL9Dialect